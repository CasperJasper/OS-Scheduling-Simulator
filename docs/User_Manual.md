
```markdown
# Quick Start Guide

1. Install: `pip install -r requirements.txt`
2. Run: `python src/main.py` 
3. Results: Check `src/data/visualizations/` for charts
4. Data: See `src/data/output/scenario_results.json` for metrics

For demo: Simply run step 2 and show the generated charts!
