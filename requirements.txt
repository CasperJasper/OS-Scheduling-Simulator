# OS Scheduling Simulator - COMP3320 Final Project
# Core dependencies for running the simulation and generating visualizations

# Minimal requirements for OS Scheduling Simulator
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0

# Development dependencies (not required for running, but useful for development)
# pip install -r requirements.txt
